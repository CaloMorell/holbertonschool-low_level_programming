#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string, followerd by a new line.
 * @str: character
 */

void _puts(char *str)
{
	int i;

	for (i = o; str[i] != '\0' ; i++)
	;
	_putchar('\n');
}
